div(ng-include='"components/navbar/navbar.html"')

.container
  .row
    .col-lg-12
      a.btn.btn-warning.pull-right(ng-click="updateTwits()") Update data
      h1.page-header(ng-show="starred") Starred Twits ({{starred.length}}):
      h1.page-header(ng-hide="starred") Loading...
  .row(ng-if="starred")
    .col-lg-12
      table.table.table-striped
        tr
          th #
          th twit
          th date time
          th action
        tr(ng-repeat='item in starred | orderBy: "created_at":true')
          td {{::$index + 1}}
          td(ng-bind-html="item.twit.text")
          td {{item.created_at}}
          td
            .btn.btn-danger(ng-click="deleteTwit(item)")
              i.fa.fa-star

footer.footer
  .container
    p
      | &copy; wegorelax